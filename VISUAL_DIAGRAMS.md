# 📊 VISUAL DIAGRAMS - KANBAN MIGRATION

This document contains visual diagrams to help understand the migration from localStorage to MySQL database.

---

## 🔄 SYSTEM ARCHITECTURE COMPARISON

### ❌ OLD SYSTEM (localStorage-based)

```
┌─────────────────────────────────────────────────────────────┐
│                        USER DEVICE                          │
│                                                             │
│  ┌──────────────┐           ┌──────────────┐              │
│  │   Browser    │           │  Browser     │              │
│  │   Chrome     │           │  Firefox     │              │
│  └──────┬───────┘           └──────┬───────┘              │
│         │                          │                       │
│         │                          │                       │
│  ┌──────▼────────┐          ┌─────▼────────┐             │
│  │ localStorage  │          │ localStorage │             │
│  │               │          │              │             │
│  │ Board A       │          │ Board B      │             │
│  │ └─Column 1    │   ≠      │ └─Column X   │     ← DIFFERENT!
│  │   └─Card 1    │          │   └─Card Y   │             │
│  └───────────────┘          └──────────────┘             │
│                                                             │
│  ❌ No API calls                                           │
│  ❌ No backend integration                                 │
│  ❌ Data isolated per browser                              │
│  ❌ No sync between devices                                │
└─────────────────────────────────────────────────────────────┘
```

### ✅ NEW SYSTEM (Database-based)

```
┌─────────────────┐         ┌─────────────────┐
│  Device 1       │         │  Device 2       │
│  (Desktop)      │         │  (Mobile)       │
│                 │         │                 │
│  Browser Chrome │         │  Browser Safari │
└────────┬────────┘         └────────┬────────┘
         │                           │
         │  Login with same          │
         │  Google Account           │
         │                           │
         └───────────┬───────────────┘
                     │
                     │  HTTPS API Calls
                     │  (with session cookie)
                     │
         ┌───────────▼────────────┐
         │                        │
         │   LARAVEL BACKEND      │
         │                        │
         │  ┌──────────────────┐  │
         │  │ BoardController  │  │
         │  │ ColumnController │  │
         │  │ CardController   │  │
         │  └────────┬─────────┘  │
         │           │            │
         │           │ Eloquent   │
         │           │ ORM        │
         │           │            │
         │  ┌────────▼─────────┐  │
         │  │  MySQL Database  │  │
         │  │                  │  │
         │  │  ┌────────────┐  │  │
         │  │  │ users      │  │  │
         │  │  │ ├─ id      │  │  │
         │  │  │ └─ email   │  │  │
         │  │  └─────┬──────┘  │  │
         │  │        │         │  │
         │  │  ┌─────▼──────┐  │  │
         │  │  │ boards     │  │  │
         │  │  │ ├─ user_id │  │  │
         │  │  └─────┬──────┘  │  │
         │  │        │         │  │
         │  │  ┌─────▼──────┐  │  │
         │  │  │ columns    │  │  │
         │  │  │ ├─board_id │  │  │
         │  │  └─────┬──────┘  │  │
         │  │        │         │  │
         │  │  ┌─────▼──────┐  │  │
         │  │  │ cards      │  │  │
         │  │  │ ├─column_id│  │  │
         │  │  └────────────┘  │  │
         │  └──────────────────┘  │
         └─────────────────────────┘
                     │
                     │
         ┌───────────┴───────────┐
         │                       │
         │  SAME DATA            │
         │  returned to          │
         │  both devices         │
         │                       │
         └───────────────────────┘
```

---

## 🔐 AUTHENTICATION FLOW

```
┌──────────────┐
│    User      │
│ Opens App    │
└──────┬───────┘
       │
       │ Click "Login with Google"
       │
       ▼
┌──────────────────────────────────┐
│   Frontend (React)               │
│   Redirects to:                  │
│   /auth/google                   │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│   Backend (Laravel)              │
│   Redirects to:                  │
│   Google OAuth Screen            │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│   Google OAuth                   │
│   User authorizes                │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│   Backend (Laravel)              │
│   /auth/google/callback          │
│                                  │
│   1. Receives Google token       │
│   2. Get user profile            │
│   3. Create/update user in DB    │
│   4. Create session cookie       │
│   5. Redirect to frontend        │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│   Frontend (React)               │
│   /auth/success                  │
│                                  │
│   1. Call /api/auth/me           │
│   2. Store user in context       │
│   3. Redirect to /kanban         │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│   Kanban Page Loads              │
│   GET /api/boards                │
│   (session cookie sent           │
│    automatically)                │
└──────────────────────────────────┘
```

---

## 📊 DATA FLOW: CREATE BOARD

```
┌──────────────────────────────────────────────────────────────┐
│                        STEP 1: USER INPUT                    │
└──────────────────────────────────────────────────────────────┘

User clicks "Create Board" → Enters title "My Work"


┌──────────────────────────────────────────────────────────────┐
│                   STEP 2: FRONTEND CALL                      │
└──────────────────────────────────────────────────────────────┘

React Component:
  ↓
BoardSelector.tsx → handleCreateBoard()
  ↓
Call: createBoard("My Work")
  ↓
kanban.api.ts:
  await axios.post('/api/boards', { 
    title: "My Work" 
  }, {
    withCredentials: true  ← Send session cookie
  })


┌──────────────────────────────────────────────────────────────┐
│                   STEP 3: BACKEND PROCESSING                 │
└──────────────────────────────────────────────────────────────┘

Laravel receives POST /api/boards:
  ↓
1. Check auth:sanctum middleware
   - Validate session cookie
   - Get Auth::id() → user_id = 123
  ↓
2. BoardController@store():
   - Validate request (title required)
   - Create board:
     Board::create([
       'user_id' => 123,
       'title' => 'My Work'
     ])
  ↓
3. MySQL INSERT:
   INSERT INTO boards (user_id, title, created_at)
   VALUES (123, 'My Work', NOW())
  ↓
4. Return response:
   {
     "success": true,
     "data": {
       "id": 1,
       "user_id": 123,
       "title": "My Work",
       "created_at": "2025-12-31 10:00:00"
     }
   }


┌──────────────────────────────────────────────────────────────┐
│                   STEP 4: FRONTEND UPDATE                    │
└──────────────────────────────────────────────────────────────┘

React receives response:
  ↓
Update state:
  setBoards(prev => [...prev, newBoard])
  ↓
UI updates:
  - Board appears in sidebar
  - User can select it
  - Kanban board loads
```

---

## 🔄 DATA FLOW: SYNC BETWEEN DEVICES

```
┌─────────────────────────────────────────────────────────────────┐
│                        SCENARIO                                 │
│                                                                 │
│  User creates board on Desktop, then opens Mobile app          │
└─────────────────────────────────────────────────────────────────┘


DEVICE A (Desktop)                    DEVICE B (Mobile)
       │                                     │
       │                                     │
┌──────▼────────┐                    ┌──────▼────────┐
│ Login Google  │                    │ Login Google  │
│ email@test.com│                    │ email@test.com│
└──────┬────────┘                    └──────┬────────┘
       │                                     │
       │                                     │
       │         ┌───────────────┐          │
       ├────────►│    Backend    │◄─────────┤
       │         │  Auth::id()   │          │
       │         │  = 123        │          │
       │         └───────┬───────┘          │
       │                 │                  │
       │                 │                  │
       │         ┌───────▼────────┐         │
       │         │  MySQL Database│         │
       │         │                │         │
       │         │  users:        │         │
       │         │  └─id: 123     │         │
       │         │    email: ...  │         │
       │         └────────────────┘         │
       │                                     │
       │                                     │
┌──────▼────────┐                           │
│ Create Board  │                           │
│ "My Tasks"    │                           │
└──────┬────────┘                           │
       │                                     │
       │ POST /api/boards                   │
       │ {title: "My Tasks"}                │
       │                                     │
       │         ┌───────────────┐          │
       ├────────►│    Backend    │          │
       │         │  user_id=123  │          │
       │         └───────┬───────┘          │
       │                 │                  │
       │                 │ INSERT           │
       │         ┌───────▼────────┐         │
       │         │  MySQL Database│         │
       │         │                │         │
       │         │  boards:       │         │
       │         │  └─id: 1       │         │
       │         │    user_id: 123│         │
       │         │    title: "..."│         │
       │         └────────────────┘         │
       │                                     │
       │                                     │
┌──────▼────────┐                    ┌──────▼────────┐
│ Board created │                    │ Open Kanban   │
│ Shows in list │                    │ page          │
└───────────────┘                    └──────┬────────┘
                                            │
                                            │ GET /api/boards
                                            │
                                     ┌──────▼────────┐
                                     │    Backend    │
                                     │  Query:       │
                                     │  WHERE        │
                                     │  user_id=123  │
                                     └──────┬────────┘
                                            │
                                            │ SELECT
                                     ┌──────▼────────┐
                                     │  MySQL Database│
                                     │                │
                                     │  Return:       │
                                     │  [             │
                                     │    {id: 1,     │
                                     │     title:"..."│
                                     │     user_id:123│
                                     │    }           │
                                     │  ]             │
                                     └──────┬────────┘
                                            │
                                     ┌──────▼────────┐
                                     │ ✅ Same board │
                                     │ appears!      │
                                     │ "My Tasks"    │
                                     └───────────────┘
```

---

## 🛠️ CODE FLOW: useKanban Hook

```
┌──────────────────────────────────────────────────────────────┐
│                Component renders                             │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  const { board, loading, addColumn, ... } =                  │
│    useKanban({ boardId: "1" })                               │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  kanban.hooks.backend.ts                                     │
│                                                              │
│  1. Initialize state:                                        │
│     - board = { columns: [], cards: {} }                    │
│     - loading = false                                        │
│     - error = null                                           │
│                                                              │
│  2. useEffect runs (when boardId changes):                   │
│     - Set loading = true                                     │
│     - Call loadBoard(boardId)                                │
│                                                              │
│  3. loadBoard() function:                                    │
│     ┌─────────────────────────────────────────┐             │
│     │ const data =                            │             │
│     │   await KanbanAPI.loadBoardData(boardId)│             │
│     └─────────────────────────────────────────┘             │
│                          │                                   │
│                          ▼                                   │
│     ┌─────────────────────────────────────────┐             │
│     │ kanban.api.ts                           │             │
│     │                                         │             │
│     │ export const loadBoardData = async (id) │             │
│     │   const columns =                       │             │
│     │     await fetchBoardColumns(id)         │             │
│     │                                         │             │
│     │   for (column of columns) {             │             │
│     │     const cards =                       │             │
│     │       await fetchColumnCards(column.id) │             │
│     │   }                                     │             │
│     │                                         │             │
│     │   return { columns, cards }             │             │
│     └─────────────────────────────────────────┘             │
│                          │                                   │
│                          ▼                                   │
│     ┌─────────────────────────────────────────┐             │
│     │ Backend API Calls:                      │             │
│     │ GET /api/boards/1/columns               │             │
│     │ GET /api/columns/1/cards                │             │
│     │ GET /api/columns/2/cards                │             │
│     │ ...                                     │             │
│     └─────────────────────────────────────────┘             │
│                          │                                   │
│                          ▼                                   │
│  4. Update state:                                            │
│     - setBoard(data)                                         │
│     - setLoading(false)                                      │
│                                                              │
│  5. Return to component:                                     │
│     - board = { columns: [...], cards: {...} }              │
│     - loading = false                                        │
│     - Functions: addColumn, addCard, etc.                    │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Component re-renders with board data                        │
│  - Shows columns                                             │
│  - Shows cards                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔒 SECURITY FLOW: Authorization Check

```
┌──────────────────────────────────────────────────────────────┐
│  User A (user_id = 123) tries to access Board 1             │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Frontend: GET /api/boards/1                                 │
│  (session cookie included automatically)                     │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Backend: BoardController@show(Board $board)                 │
│                                                              │
│  Step 1: Laravel route model binding                         │
│  ┌──────────────────────────────────┐                       │
│  │ SELECT * FROM boards             │                       │
│  │ WHERE id = 1                     │                       │
│  └──────────────────────────────────┘                       │
│           │                                                  │
│           ▼                                                  │
│  $board = Board { id: 1, user_id: 123, title: "..." }       │
│                                                              │
│  Step 2: Check session cookie                                │
│  ┌──────────────────────────────────┐                       │
│  │ Auth::id() = 123                 │                       │
│  └──────────────────────────────────┘                       │
│                                                              │
│  Step 3: Authorization check                                 │
│  ┌──────────────────────────────────┐                       │
│  │ if ($board->user_id !== Auth::id()) {                    │
│  │   return 403 Unauthorized                                │
│  │ }                                                         │
│  └──────────────────────────────────┘                       │
│           │                                                  │
│           ▼                                                  │
│  ✅ $board->user_id (123) === Auth::id() (123)              │
│                                                              │
│  ✅ AUTHORIZED → Return board data                           │
└──────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────┐
│  User B (user_id = 456) tries to access Board 1             │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Backend: BoardController@show(Board $board)                 │
│                                                              │
│  $board = Board { id: 1, user_id: 123, ... }                │
│  Auth::id() = 456                                            │
│                                                              │
│  Authorization check:                                        │
│  ┌──────────────────────────────────┐                       │
│  │ if ($board->user_id !== Auth::id()) {                    │
│  │   return 403 Unauthorized        │                       │
│  │ }                                                         │
│  └──────────────────────────────────┘                       │
│           │                                                  │
│           ▼                                                  │
│  ❌ $board->user_id (123) !== Auth::id() (456)              │
│                                                              │
│  ❌ UNAUTHORIZED → Return 403 error                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 📈 PERFORMANCE: Query Optimization

```
┌──────────────────────────────────────────────────────────────┐
│  ❌ BAD: N+1 Query Problem                                   │
└──────────────────────────────────────────────────────────────┘

Load board data (inefficient):

1. SELECT * FROM boards WHERE id = 1
2. SELECT * FROM columns WHERE board_id = 1    (1 query)
3. For each column:
   - SELECT * FROM cards WHERE column_id = 1   (1 query per column)
   - SELECT * FROM cards WHERE column_id = 2   (1 query per column)
   - SELECT * FROM cards WHERE column_id = 3   (1 query per column)
   - ...

Total: 1 + 1 + N queries (if N columns)
Example: 1 board + 4 columns = 6 queries!


┌──────────────────────────────────────────────────────────────┐
│  ✅ GOOD: Eager Loading (if using Eloquent relationships)   │
└──────────────────────────────────────────────────────────────┘

$board = Board::with(['columns.cards.labels'])
              ->find($id);

1. SELECT * FROM boards WHERE id = 1
2. SELECT * FROM columns WHERE board_id IN (1)
3. SELECT * FROM cards WHERE column_id IN (1, 2, 3, 4)
4. SELECT * FROM card_labels WHERE card_id IN (1, 2, 3, ...)

Total: 4 queries (constant, regardless of data size!)


┌──────────────────────────────────────────────────────────────┐
│  ✅ CURRENT: Manual queries (acceptable for kanban.api.ts)  │
└──────────────────────────────────────────────────────────────┘

Frontend calls:
1. GET /api/boards/1/columns  → 1 query
2. For each column (parallel requests):
   - GET /api/columns/1/cards  → 1 query
   - GET /api/columns/2/cards  → 1 query
   - GET /api/columns/3/cards  → 1 query

Total: 1 + N queries (parallel)
With proper indexing (board_id, column_id) → very fast!
```

---

## 🗄️ DATABASE RELATIONSHIPS

```
┌─────────────────────────────────────────────────────────────┐
│                     USER (Google OAuth)                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ users                                                  │  │
│  │ ├─ id (PK)           : 123                            │  │
│  │ ├─ email             : user@example.com               │  │
│  │ ├─ name              : John Doe                       │  │
│  │ └─ google_id         : 1234567890                     │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           │ 1 user → many boards
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                          BOARDS                             │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │ boards (id: 1)   │  │ boards (id: 2)   │               │
│  │ ├─ id (PK)       │  │ ├─ id (PK)       │               │
│  │ ├─ user_id (FK)  │  │ ├─ user_id (FK)  │               │
│  │ └─ title         │  │ └─ title         │               │
│  └────────┬─────────┘  └────────┬─────────┘               │
└───────────┼────────────────────┼─────────────────────────┘
            │                    │
            │ 1 board →          │
            │ many columns       │
            │                    │
┌───────────▼──────────┬─────────▼──────────┐
│       COLUMNS        │       COLUMNS      │
│  ┌────────────────┐  │  ┌────────────────┐│
│  │columns (id: 1) │  │  │columns (id: 3) ││
│  │├─ id (PK)      │  │  │├─ id (PK)      ││
│  │├─ board_id (FK)│  │  │├─ board_id (FK)││
│  │├─ title        │  │  │├─ title        ││
│  │├─ color        │  │  │├─ color        ││
│  │└─ position     │  │  │└─ position     ││
│  └────────┬───────┘  │  └────────┬───────┘│
│           │          │           │        │
│  ┌────────────────┐  │  ┌────────────────┐│
│  │columns (id: 2) │  │  │columns (id: 4) ││
│  │├─ id (PK)      │  │  │├─ id (PK)      ││
│  │├─ board_id (FK)│  │  │├─ board_id (FK)││
│  │├─ title        │  │  │├─ title        ││
│  │└─ position     │  │  │└─ position     ││
│  └────────┬───────┘  │  └────────┬───────┘│
└───────────┼──────────┴───────────┼────────┘
            │                      │
            │ 1 column →           │
            │ many cards           │
            │                      │
┌───────────▼─────────┬────────────▼────────┐
│         CARDS       │         CARDS       │
│  ┌───────────────┐  │  ┌───────────────┐  │
│  │cards (id: 1)  │  │  │cards (id: 3)  │  │
│  │├─ id (PK)     │  │  │├─ id (PK)     │  │
│  │├─ column_id   │  │  │├─ column_id   │  │
│  │├─ title       │  │  │├─ title       │  │
│  │├─ description │  │  │├─ description │  │
│  │├─ due_date    │  │  │├─ due_date    │  │
│  │└─ position    │  │  │└─ position    │  │
│  └───────┬───────┘  │  └───────┬───────┘  │
│          │          │          │          │
│  ┌───────────────┐  │  ┌───────────────┐  │
│  │cards (id: 2)  │  │  │cards (id: 4)  │  │
│  │├─ id (PK)     │  │  │├─ id (PK)     │  │
│  │├─ column_id   │  │  │├─ column_id   │  │
│  │├─ title       │  │  │├─ title       │  │
│  │└─ position    │  │  │└─ position    │  │
│  └───────┬───────┘  │  └───────┬───────┘  │
└──────────┼──────────┴──────────┼──────────┘
           │                     │
           │ 1 card →            │
           │ many labels         │
           │                     │
┌──────────▼──────────┬──────────▼──────────┐
│    CARD LABELS      │    CARD LABELS      │
│  ┌───────────────┐  │  ┌───────────────┐  │
│  │card_labels    │  │  │card_labels    │  │
│  │├─ id (PK)     │  │  │├─ id (PK)     │  │
│  │├─ card_id (FK)│  │  │├─ card_id (FK)│  │
│  │└─ label_name  │  │  │└─ label_name  │  │
│  │   "Design"    │  │  │   "Urgent"    │  │
│  └───────────────┘  │  └───────────────┘  │
└─────────────────────┴─────────────────────┘
```

---

## 🎯 SUCCESS CRITERIA VISUALIZATION

```
┌─────────────────────────────────────────────────────────────┐
│                   BEFORE vs AFTER                           │
└─────────────────────────────────────────────────────────────┘

BEFORE (❌):
User logs in
     ↓
localStorage checked
     ↓
Empty? → Create 4 columns + 5 cards (hardcoded)
     ↓
User sees pre-filled board
     ↓
User makes changes
     ↓
Saved to localStorage only
     ↓
Login on another device → Different data ❌


AFTER (✅):
User logs in (Google OAuth)
     ↓
Backend verifies session → user_id = 123
     ↓
Frontend: GET /api/boards
     ↓
Backend: SELECT * FROM boards WHERE user_id = 123
     ↓
Result: [] (empty array)
     ↓
User sees EMPTY board ✅
     ↓
User clicks "Create Board"
     ↓
Frontend: POST /api/boards {title: "Work"}
     ↓
Backend: INSERT INTO boards (user_id, title) VALUES (123, "Work")
     ↓
Board created in MySQL ✅
     ↓
User adds columns & cards
     ↓
All saved to MySQL ✅
     ↓
Login on another device (same Google account)
     ↓
Backend: SELECT * FROM boards WHERE user_id = 123
     ↓
Returns same board data
     ↓
Same kanban appears on both devices ✅
```

---

**End of Visual Diagrams**

These diagrams should help visualize the migration process and understand how the new system works.
